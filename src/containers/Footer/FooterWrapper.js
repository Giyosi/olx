import styled from "styled-components";

const FooterWrapper = styled.div`

    background: ${props=>props.colors.footerBack} ;

    a {
        color: white;
        font-weight: bold;
    }

`;

export default FooterWrapper;






